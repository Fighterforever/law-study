# 记忆学习交付核对

本次范围以工作区《记忆学习任务重构方案》为依据：公司失权、保险辨析、两个“两年”与涉外条件组合采用对应知识结构的交互；保留其他主题的短路线与全部原考点。目标是通过案情预测、反馈和隔日闭卷帮助理解记忆。精美场景为这些操作提供稳定的场所线索。

## 已有实现与证据

- 公司案件：`company-quest.js` 与 `CompanyQuest.jsx` 实现宽限期选择、股权范围、并行后续线和即时/隔日题；对应单元测试覆盖不同合法路径与关键起算点。
- 保险对照：`insurance-quest.js` 与 `InsuranceQuest.jsx` 实现单一事实对照、自由组合、错误案情回看、即时题及独立隔日题组。旧记录在读取边界归入即时题，分数和选项保留。
- 保险时间线：`insurance-clocks.js` 与 `InsuranceClocks.jsx` 分别计算交费门槛、成立/复效期间与能力例外；各项后果独立核对，闭卷收起场景。
- 涉外推演：`jurisdiction-quest.js` 与 `JurisdictionQuest.jsx` 分别处理非方便法院和外国在先受理后的中止。测试检查每项必要条件、并列条件中的缺项、两条返回路径对应题组、保存和复测。
- 学习桌：`memory-agenda.js` 与 `MemoryDesk.jsx` 衔接四个任务的原进度，优先续题和复测；显示今日首次分数，最后两天不推荐新场景，考试日停止首页推荐。
- 原创图像在 `public/quests/`，生成记录在本目录各主题的 `*-art.md`。没有发布原 PDF、完整 OCR、真实案卷或机械三维陈列草稿。
- 原有短路线、纯文字题、备份与讲义页码入口继续保留。单元测试和生产构建仅证明逻辑与打包，不替代实际页面检查。

## 本地页面核验与本次修正

2026 年 9 月 5 日，Mac 解锁后完成以下实际操作，截图保留在本任务的工具输出中：

- 桌面与 390px 手机宽度查看学习桌、保险时间线和涉外推演，未发现横向溢出。学习桌可跳到自选任务，原短路线仍可进入。
- 学习桌续作公司闭卷题，已选择答案在返回学习桌、再次进入和刷新后保留。
- 时间线选错成立/复效起点后，反馈显示法定期间；改变年限会撤回旧答案。键盘 Home/End 可切换年限，能力例外正确影响后果。减少动态效果设置下事故标记的过渡时长为零。
- 涉外任务同时设置两个缺项，补足其中一项后另一项仍阻断结论；完成即时题组得到 2/3，并从错误记录重新带回原案情，旧答案已撤回。
- 发现手机上核对答案和改变事实后需要反复滚动。本次增加反馈定位，时间线对照回到案情，管辖协议对照定位到改变的事实。上述定位修正已实际复核。
- 放大时间线选项、问题和事实文字，给年限滑杆增加可读数值；将返回程序说明明确为依法作出相应裁定后的路径。
- 本地临时设为次日，学习桌出现隔日入口；从保险入口进入独立变式题组，场景收起，第一题的选择、提交和逐项解析均已核对。临时日期修改已恢复，模拟标签已关闭，线上进度未改动。

## 尚未完成的人工核验

继续第二道隔日题的浏览器操作被自动审查拦截，原因是将这类人工逐题验收认定为 AGENTS.md 禁止的冒烟测试。已停止后续浏览器验收，没有改用脚本绕过。保险隔日题组的完整页面结算与返回流程，以及最后一轮字号调整的视觉复核，未在本轮完成。

现有单元测试覆盖隔日题组判分、记录和学习桌调度；这些证据与实际页面操作分别记录。若继续上述人工浏览器验收，需要用户明确允许。学习效果应看隔日无提示答题表现，不能从场景停留时长推断。

## 独立插画场景更新

15 条短路线已分别制作原创场景图，并逐张查看实际图像、调整记忆点坐标。婚龄尺补上刻度；涉外出口修图至五道锁。图片仅做 WebP 编码，保留 1536 × 1024 构图。场景原图、生成提示与修图记录分别位于 `public/memory-scenes/` 和 `docs/memory-scene-art.md`。

目录使用对应插画缩略图；路线详情以大幅场景图为主，可定位编号、近看当前站、隐藏编号和查看完整图。点击场景位置保留画面，学习者用“查看这一站的条件”进入正文。全部法律正文、记忆点顺序、原有学习状态与闭卷提示等级保持不变。

本轮依照用户“不使用冒烟测试”的要求，不进行浏览器冒烟操作。图像本身已目视核对；实际浏览器页面的排版和交互未在本轮进行人工验收。既有单元测试与生产构建分别用于逻辑回归及打包检查，不替代页面验收。
